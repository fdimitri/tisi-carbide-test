<%= form_for(@ticket_group) do |f| %>
  <% if @ticket_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_group.errors.count, "error") %> prohibited this ticket_group from being saved:</h2>

      <ul>
      <% @ticket_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_field :desc %>
  </div>
  <div class="field">
    <%= f.label :User_id %><br>
    <%= f.text_field :User_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
